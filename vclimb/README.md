# Version Climber on OpenAlea Release

## Create a conda environment

mamba create -n vclimb versionclimber -c versionclimber
mamba activate versionclimber

## Configuration

The configuration is defined in config.yaml file.

## Run the configuration
